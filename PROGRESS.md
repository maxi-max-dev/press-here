# PROGRESS
- 目标：完成“按这里 / PRESS HERE”本地交互 Demo，跑通样例、自定义手动标点、编辑、指南与 QR 闭环。
- 顺序：黄金路径 → 自定义诚实边界 → 测试与双视口验收 → 文档交付。
- 最大风险：图片上的可拖拽标点、持久化校验、移动指南路由与 QR 编码必须同时真实可用。
- 栈：React + TypeScript + Vite；状态存 localStorage，二维码使用可解码的本地库。
- 任务 0：目录为空；已复核 Node v22.23.1、npm 10.9.8。
- 第 1 轮：已完成黄金路径、自定义上传/手动标点、独立指南路由、持久化校验与 QR 解码测试；进入首次验收。
- 第 1 轮验收：lint 首次失败（effect 内同步恢复状态、内部链接规则）；已改为异步恢复和框架链接。
- 第 2 轮：浏览器发现 Canvas 二维码在当前环境未呈现；改为同库 SVG 输出，保留独立库解码验证。
- 第 2 轮复验：重启受依赖变更污染的开发服务后，二维码呈现；样例切换、编辑、键盘移点、保存刷新、独立指南与完成状态均通过。
- 第 3 轮：双视口无横向溢出；隐藏视觉滚动条但保留滚动，确保截图文件严格匹配 1440×900 / 390×844。
- 第 3 轮审计修复：自定义上传后自动进入编辑模式；标点自身捕获拖拽指针，避免松手误新增步骤。
- 第 3 轮审计修复：照片区域允许纵向触摸滚动；未保存编辑不显示旧二维码；指南遇损坏持久化数据直接报错。
- 第 3 轮测试补强：新增样例标点、编辑全操作、真实 QR 组件、空文案、可见损坏警告与指南完成/损坏拒绝回归；未删减既有测试。
- 第 3 轮诚实性修复：无效活动项目不再静默回退；明确自定义二维码不携带跨设备本地数据；超大说明文件可重新选择。
- 第 3 轮坐标修复：自定义 contain 图片按真实内容区换算标点，点击 letterbox 留白会显示错误且不能生成假点。
- 第 3 轮测试首跑：13 项中 1 项因测试未先选中活动步骤而红；补真实点击后保留排序与坐标强断言。
- 第 4 轮反向验证：故意把越界错误文案断言改错，定向测试 exit 1；红色输出保存到 artifacts/reverse-verification-red.txt，原断言已立即恢复。
- 第 4 轮恢复：完整测试 3 files / 13 tests 全绿；输出保存到 artifacts/reverse-verification-green.txt。
- 第 5 轮视觉验收：浏览器实走 1440×900 / 390×844，无横向溢出；桌面、移动首页、自定义编辑、指南与完成截图已保存。
- Sites 社交图：一次生成出现假装饰码而拒用；唯一一次修正移除假码，定稿为 public/og.png（1200×630），元数据按请求 host 生成绝对 URL。
- 清理：替换 starter favicon 为“按”品牌标记，删除 3 个未使用 starter 图标；依赖与素材均保持项目必需范围。
- 最终门禁：npm run lint exit 0；npm test 3 files / 13 tests 全绿；npm run build exit 0（/ 与 /guide/:id）。
- 最终视口：浏览器 1440×900 与 390×844 均 scrollWidth===clientWidth；5 张正式 PNG 尺寸复核通过，控制台 0 warning / 0 error。
- 完成条件：两个示例与自定义手动路径均实走；编辑/拖拽/排序/保存刷新/QR/移动完成闭环通过。遗留仅为已明确披露的“自定义数据不跨浏览器”。
- 新目标第 1 轮审计：新增任务明确不部署；此前 GitHub 仅做只读授权/兼容性检查，未创建仓库、提交、推送或部署，失败补丁未写入文件。
- 新目标第 1 轮模型：Workspace 升为 v2，继续读取原存储键；v1 项目会保留图片、说明、步骤 ID/顺序/文案/坐标并补齐标题、提示类型与本机反馈状态。
- 新目标第 1 轮交互：两个样例补齐步骤标题与提示/警告；编辑器新增对应字段、待修正提示、明确解决按钮和样例重置。
- 新目标第 1 轮闭环：移动指南新增“这一步不对 / 已解决”本机反馈并持久化；作者端可见待修正并显式标记修正，删除或重置步骤会自然清除绑定状态。
- 新目标第 1 轮视觉：增加 5 秒产品定义任务带、自定义空画布上传入口、实体打印感分享卡、可辨识提示/警告与移动端粘性操作栏。
- 新目标第 2 轮测试：原 13 项全部保留，新增迁移、标题/警告编辑、重置、分享入口、阅读反馈与作者解决闭环；再补完整读者反馈→作者修正→阅读端确认测试，现为 4 files / 25 tests 全绿。
- 新目标第 2 轮空状态：修复无步骤时反馈条件导致的崩溃；无效自定义项目的分享卡显示二维码生成条件，不再留空列。
- 新目标第 2 轮环境恢复：macOS 将未改配置与部分依赖转为 iCloud 空占位；已从本轮读取内容恢复配置，并用完整 package-lock + npm ci 重建可再生依赖，不涉及其他项目。
- 新目标第 2 轮构建：npm run build exit 0；生产路由 / 与 /guide/:id 构建完成。
- 新目标第 3 轮浏览器：真实上传本地设备图并点击蓝色电源控件，填写标题、动作、警告与说明参考；保存刷新后图片、坐标和文字恢复。
- 新目标第 3 轮反馈：390×844 阅读端提交“这一步不对”，1440×900 作者端修改并标记已修正，再开手机页看到新文案与“已解决”，最终走到 3 / 3 完成。
- 新目标第 3 轮视口：增强版桌面首屏、自定义手动标注、反馈前后和完成页截图已保存；实测 scrollWidth===clientWidth，控制台 0 warning / 0 error。
- 新目标最终门禁：npm run lint exit 0；npm test 4 files / 25 tests 全绿；npm run build exit 0。遗留仅为 README 已披露的本机 localStorage、无跨设备同步/公网分享。
- 补救重建：原 Documents 目录出现 dataless 占位；已改在 `/Users/max/code/press-here` 从物化构建产物、可读配置与逐文件源码重建，明确不带 node_modules、dist、.git，不安装、不推送、不部署。
